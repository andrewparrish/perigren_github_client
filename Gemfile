source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'excon'
gem 'openssl'
gem 'jwt'

# Specify your gem's dependencies in perigren_github_client.gemspec
gemspec
